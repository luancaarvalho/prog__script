import { Tree } from '@angular-devkit/schematics';
/**
 * Adds a script to the package.json
 */
export declare function addNpmScriptToPackageJson(host: Tree, name: string, command: string): Tree;
